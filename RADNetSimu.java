package traceur_de_paquet;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Arrays;

public class RADNetSimu {
	public static void main(String[] args) throws IOException {
		
		/*AddrIp ipaddr1 = new AddrIp();
		AddrMac macaddr1 = new AddrMac();
		
		short[] ip1 = {0,0,0,0};
		BufferedReader reader=new BufferedReader(new InputStreamReader(System.in));
		for(int n=0;n<4;n++) {
			short ipelem=Short.parseShort(reader.readLine());
			ip1[n]=ipelem;
		}
		
		short[] mask1={0,0,0,0};
		for(int n=0;n<4;n++) {
			short maskelem=Short.parseShort(reader.readLine());
			mask1[n]=maskelem;
		}
		
		ipaddr1.setIp(ip1);
		ipaddr1.setMask(mask1);
		System.out.println(Arrays.toString(ipaddr1.getIp()));
		System.out.println(Arrays.toString(ipaddr1.getMask()));
		String[] mac1={"ef","ae","d7","fd","29","38"};
		macaddr1.setMac(mac1);
		System.out.println(Arrays.toString(macaddr1.getMac()));*/
	}
}
